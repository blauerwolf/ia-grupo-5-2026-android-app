## 1. Core Implementation

- [x] 1.1 Modificar `lib/main.dart` para cambiar el ícono condicional en `_buildHeader()` por `Icons.switch_camera_rounded` estático.

## 2. Verification

- [x] 2.1 Ejecutar `flutter analyze` para verificar la ausencia de errores estáticos.
- [x] 2.2 Ejecutar las pruebas unitarias y de widgets con `flutter test` para validar que la UI y lógica se mantengan correctas.
